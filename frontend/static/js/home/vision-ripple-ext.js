VisionCardReveal.prototype.animateContentIn=function(){const animDuration=600;this.cards.forEach((card,i)=>{const delay=10+i*120;this._later(()=>{card.classList.add('summoned');},delay);this._later(()=>{this.spawnMeshBurst(card);},delay+animDuration*0.35);});this._later(()=>{this.cards.forEach(card=>{card.style.opacity='1';card.style.transform='scale(1) translateY(0)';card.style.animation='none';});},900);};VisionCardReveal.prototype.resetPage2=function(){this._pendingTimers.forEach(id=>clearTimeout(id));this._pendingTimers.length=0;const section=document.getElementById('vision');if(section){section.classList.remove('h2-grid-ready');}
this.cards.forEach(card=>{card.classList.remove('summoned');card.style.opacity='';card.style.transform='';card.style.animation='';});};
